<% csv_file   = File.join(__dir__, 'recipes.csv') %>
<% cookbook   = Cookbook.new(csv_file) %>

<!-- # Ecrire la liste
  new_list = cookbook.all
  new_list.each do |recipe|
    puts "#{new_list.index(recipe)} - #{recipe.status} - #{recipe.name} - ⏲️ #{recipe.time}"
  end
 -->

